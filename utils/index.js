const response = require("./responseMessage");
const multerUpload = require("./multerStorage");
const cloudy = require("./cloudinaryStorage");
const res = require("./result");

module.exports = { response, multerUpload, cloudy, res };
